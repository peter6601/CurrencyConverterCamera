# Git Commit 工作流程圖

## 整體流程概覽

```
┌──────────────────────────────────────────────────────────────────┐
│                   Git Commit 工作流程                              │
└──────────────────────────────────────────────────────────────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  Step 1: .gitignore   │
                    │  忽略 specs 文件       │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  Step 2: Models       │
                    │  ✅ 無測試失敗         │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  Step 3: Utilities    │
                    │  ✅ 無測試失敗         │
                    └───────────┬───────────┘
                                │
                ┌───────────────┴───────────────┐
                │                               │
                ▼                               ▼
    ┌───────────────────────┐      ┌───────────────────────┐
    │  選項 A: 先修復        │      │  選項 B: 先 commit    │
    │  (推薦用於生產)        │      │  (快速記錄進度)        │
    └───────────┬───────────┘      └───────────┬───────────┘
                │                               │
                ▼                               ▼
    ┌───────────────────────┐      ┌───────────────────────┐
    │ 修復所有測試失敗       │      │ Commit 所有模組        │
    │ (22 個測試)            │      │ (標註已知問題)         │
    └───────────┬───────────┘      └───────────┬───────────┘
                │                               │
                ▼                               ▼
    ┌───────────────────────┐      ┌───────────────────────┐
    │ Commit 所有模組        │      │ 創建修復 commits       │
    │ (測試全部通過)         │      │ (逐一修復問題)         │
    └───────────┬───────────┘      └───────────┬───────────┘
                │                               │
                └───────────────┬───────────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  完成: 查看歷史        │
                    │  git log --oneline    │
                    └───────────────────────┘
```

---

## 詳細模組 Commit 順序

```
階段 1: 基礎設施 (立即可 commit)
┌─────────────────────────────────────────────────┐
│ 1. .gitignore                                   │
│    ✅ 忽略 specs/, PHASE_*.md, *.md            │
│    └─> git add .gitignore                       │
│        git commit -m "chore: add .gitignore"    │
├─────────────────────────────────────────────────┤
│ 2. Models/                                      │
│    ✅ CurrencySettings, ConversionRecord       │
│    ✅ DetectedNumber                            │
│    ✅ 無測試失敗                                │
│    └─> git add Models/                          │
│        git commit -m "feat(models): ..."        │
├─────────────────────────────────────────────────┤
│ 3. Utilities/                                   │
│    ✅ Constants, Extensions, Logger            │
│    ✅ 無測試失敗                                │
│    └─> git add Utilities/                       │
│        git commit -m "feat(utilities): ..."     │
└─────────────────────────────────────────────────┘
                        │
                        ▼
階段 2: 服務層 (有測試失敗)
┌─────────────────────────────────────────────────┐
│ 4. Services/StorageService.swift                │
│    ⚠️  3 個測試失敗                             │
│    - testHistoryRetentionPolicy (54 vs 50)      │
│    - testSaveCurrencySettingsUpdatesTimestamp   │
│    - testSettingsPersistAcrossInstances         │
│    └─> 建議: 先修復再 commit                    │
├─────────────────────────────────────────────────┤
│ 5. Services/ConversionEngine.swift              │
│    ✅ 無測試失敗                                │
│    └─> git add Services/ConversionEngine.swift  │
├─────────────────────────────────────────────────┤
│ 6. Services/CameraService.swift                 │
│    ⚠️  2 個測試失敗                             │
│    - testSessionHasVideoInput (0 inputs)        │
│    - testSessionHasVideoOutput (0 outputs)      │
│    └─> 建議: 先修復再 commit                    │
├─────────────────────────────────────────────────┤
│ 7. Services/VisionService.swift                 │
│    ⚠️  3 個測試失敗                             │
│    - testRecognitionReturnsArray (連接丟失)     │
│    - testRecognizeTextFromPixelBuffer           │
│    - testTextRecognitionPerformance (取消)      │
│    └─> 建議: 先修復再 commit                    │
└─────────────────────────────────────────────────┘
                        │
                        ▼
階段 3: MVVM 層 (有測試失敗)
┌─────────────────────────────────────────────────┐
│ 8. ViewModels/SettingsViewModel.swift           │
│    🔥 8 個測試失敗 - CRITICAL                   │
│    - testEmptyCurrencyName                      │
│    - testCurrencyNameWithNumbers                │
│    - testCurrencyNameTooLong                    │
│    - testInvalidExchangeRateFormat              │
│    - testExchangeRateZero                       │
│    - testExchangeRateTooSmall                   │
│    - testExchangeRateTooLarge                   │
│    - testValidationErrorMessage                 │
│    └─> 必須修復! 阻塞所有後續功能               │
├─────────────────────────────────────────────────┤
│ 9. ViewModels/CameraViewModel.swift             │
│    ✅ 無直接測試失敗 (依賴 Camera/Vision)       │
├─────────────────────────────────────────────────┤
│ 10. ViewModels/HistoryViewModel.swift           │
│     ⚠️  2 個測試失敗                            │
│     - testHistoryWithDifferentCurrencies        │
│     - testConcurrentRecordAddition (1 vs 10)    │
│     └─> 建議: 先修復再 commit                   │
├─────────────────────────────────────────────────┤
│ 11. Views/                                      │
│     ⚠️  1 個測試失敗                            │
│     - testFormattedOriginalPriceDisplay         │
│     └─> 可 commit,後續修復                      │
└─────────────────────────────────────────────────┘
                        │
                        ▼
階段 4: 應用程式和資源 (立即可 commit)
┌─────────────────────────────────────────────────┐
│ 12. App/                                        │
│     ✅ 應用程式入口點                           │
│     └─> git add App/                            │
├─────────────────────────────────────────────────┤
│ 13. Resources/                                  │
│     ✅ 本地化和資源                             │
│     └─> git add Resources/                      │
├─────────────────────────────────────────────────┤
│ 14. Tests/                                      │
│     ⚠️  22 個測試失敗 (彙總)                    │
│     └─> git add Tests/ (標註失敗數量)           │
└─────────────────────────────────────────────────┘
```

---

## 測試失敗修復流程

```
┌──────────────────────────────────────────────────────────────┐
│                  測試修復優先順序                               │
└──────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│ 🔥 Critical   │  │ ⚠️  High       │  │ 📝 Medium     │
│ (必須立即修復) │  │ (高優先級)     │  │ (中優先級)     │
└───────┬───────┘  └───────┬───────┘  └───────┬───────┘
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│ Settings      │  │ Camera/Vision │  │ Error Msgs    │
│ ViewModel     │  │ Services      │  │               │
│ (8 tests)     │  │ (6 tests)     │  │ (2 tests)     │
│               │  │               │  │               │
│ StorageService│  │ History       │  │ UI Format     │
│ (3 tests)     │  │ ViewModel     │  │ (1 test)      │
│               │  │ (2 tests)     │  │               │
└───────┬───────┘  └───────┬───────┘  └───────┬───────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │ 所有測試通過   │
                    │ (0 failures)  │
                    └───────────────┘
```

---

## 選項 A: 先修復再 Commit (推薦)

```
開始
  │
  ▼
┌────────────────────────────────────┐
│ 1. Commit 無問題模組               │
│    - .gitignore                    │
│    - Models/                       │
│    - Utilities/                    │
│    - ConversionEngine              │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ 2. 修復 Critical 問題               │
│    🔥 SettingsViewModel (8 tests)  │
│    🔥 StorageService (3 tests)     │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ 3. Commit 修復後的 Critical 模組    │
│    git commit -m "fix(...): ..."   │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ 4. 修復 High Priority 問題          │
│    ⚠️  Camera/Vision (6 tests)     │
│    ⚠️  HistoryViewModel (2 tests)  │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ 5. Commit 修復後的 High 模組        │
│    git commit -m "fix(...): ..."   │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ 6. 修復 Medium Priority 問題        │
│    📝 Error messages (2 tests)     │
│    📝 UI formatting (1 test)       │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ 7. Commit 所有剩餘模組              │
│    - ViewModels/                   │
│    - Views/                        │
│    - App/                          │
│    - Resources/                    │
│    - Tests/                        │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ ✅ 完成: 所有測試通過,所有代碼已   │
│    commit,commit 歷史清晰          │
└────────────────────────────────────┘
```

---

## 選項 B: 先 Commit 再修復 (快速記錄)

```
開始
  │
  ▼
┌────────────────────────────────────┐
│ 1. 運行自動化腳本                   │
│    ./git-commit-sequence.sh        │
│    (或手動 commit 所有模組)         │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ 2. 所有模組已 commit                │
│    ⚠️  Commit messages 標註已知問題 │
│    ⚠️  22 個測試失敗記錄在 message  │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ 3. 修復 Critical 問題               │
│    🔥 SettingsViewModel            │
│    🔥 StorageService               │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ 4. 創建修復 commit                  │
│    git commit -m "fix(viewmodels): │
│      fix SettingsViewModel         │
│      validation (8 tests fixed)"   │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ 5. 重複修復其他問題                 │
│    - StorageService (3 tests)      │
│    - Camera/Vision (6 tests)       │
│    - HistoryViewModel (2 tests)    │
│    - Others (3 tests)              │
│    每個問題一個 fix commit          │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ ✅ 完成: 所有測試通過,commit 歷史   │
│    顯示問題和修復過程              │
└────────────────────────────────────┘
```

---

## Git 歷史對比

### 選項 A 的 Commit 歷史 (推薦)
```
* abc1234 feat(resources): add localization and assets
* def5678 feat(app): configure app entry point
* ghi9012 feat(views): implement SwiftUI UI components
* jkl3456 feat(viewmodels): implement ViewModels (all tests passing)
* mno7890 feat(services): implement VisionService (all tests passing)
* pqr1234 feat(services): implement CameraService (all tests passing)
* stu5678 feat(services): implement ConversionEngine
* vwx9012 feat(services): implement StorageService (all tests passing)
* yza3456 feat(utilities): add utility functions and extensions
* bcd7890 feat(models): implement core data models
* efg1234 chore: add comprehensive .gitignore
```
**優點**: 每個 commit 都是可工作狀態,測試全部通過,歷史乾淨

---

### 選項 B 的 Commit 歷史
```
* xyz9876 fix(views): fix price formatting display (1 test fixed)
* wvu6543 fix(viewmodels): fix HistoryViewModel concurrency (2 tests fixed)
* tsr3210 fix(services): fix Camera and Vision integration (6 tests fixed)
* qpo0987 fix(services): fix StorageService persistence (3 tests fixed)
* nml6543 fix(viewmodels): fix SettingsViewModel validation (8 tests fixed)
* kji3210 test: add comprehensive test suite (⚠️ 22 failures)
* hgf0987 feat(resources): add localization and assets
* edc6543 feat(app): configure app entry point
* baz3210 feat(views): implement SwiftUI UI components (⚠️ 1 failure)
* yxw0987 feat(viewmodels): implement ViewModels (⚠️ 10 failures)
* vut6543 feat(services): implement VisionService (⚠️ 3 failures)
* srq3210 feat(services): implement CameraService (⚠️ 2 failures)
* pon0987 feat(services): implement ConversionEngine
* mlk6543 feat(services): implement StorageService (⚠️ 3 failures)
* jih3210 feat(utilities): add utility functions
* gfe0987 feat(models): implement core data models
* dcb6543 chore: add .gitignore
```
**優點**: 快速記錄所有進度,修復歷史清晰可見  
**缺點**: 中間有多個 commits 包含失敗測試

---

## 自動化腳本流程

```
./git-commit-sequence.sh
         │
         ▼
┌────────────────────┐
│ 檢查目錄是否正確    │
└────────┬───────────┘
         │ ✅
         ▼
┌────────────────────┐
│ Stage 1: .gitignore│
│ 詢問: 是否繼續? y/n │
└────────┬───────────┘
         │ y
         ▼
┌────────────────────┐
│ git add .gitignore │
│ git commit         │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Stage 2: Models/   │
│ 詢問: 是否繼續? y/n │
└────────┬───────────┘
         │ y
         ▼
┌────────────────────┐
│ git add Models/    │
│ git commit         │
└────────┬───────────┘
         │
         ▼
   (重複 12 個 stages)
         │
         ▼
┌────────────────────┐
│ 顯示完成訊息和統計  │
│ 提供下一步建議      │
└────────────────────┘
```

---

## 驗證流程

```
每個 Commit 之前
       │
       ▼
┌──────────────────┐
│ 檢查代碼編譯     │
│ ⌘B in Xcode     │
└─────┬────────────┘
      │ ✅
      ▼
┌──────────────────┐
│ 運行相關測試     │
│ ⌘U in Xcode     │
└─────┬────────────┘
      │
      ├─✅ 測試通過
      │    │
      │    ▼
      │ ┌──────────────────┐
      │ │ git add <files>  │
      │ │ git commit       │
      │ └──────────────────┘
      │
      └─❌ 測試失敗
           │
           ▼
        ┌──────────────────┐
        │ 決定:            │
        │ 1. 修復後 commit │
        │ 2. 標註問題 commit│
        └──────────────────┘
```

---

## 最終驗證 Checklist

```
┌─────────────────────────────────────────────┐
│ Git 結構                                     │
├─────────────────────────────────────────────┤
│ ✅ .gitignore 已創建並 commit                │
│ ✅ specs/ 文件被忽略                         │
│ ✅ 每個功能模組有獨立 commit                 │
│ ✅ Commit messages 清晰規範                  │
├─────────────────────────────────────────────┤
│ 代碼質量                                     │
├─────────────────────────────────────────────┤
│ ⬜ 所有測試通過 (當前: 22 失敗)              │
│ ⬜ 代碼覆蓋率 >85%                           │
│ ⬜ 無編譯器警告                              │
│ ⬜ 應用程式可運行                            │
├─────────────────────────────────────────────┤
│ 文檔                                        │
├─────────────────────────────────────────────┤
│ ✅ Git commit 指南完整                       │
│ ✅ 自動化腳本可用                            │
│ ✅ 快速參考指南清晰                          │
│ ✅ 工作流程圖清晰                            │
└─────────────────────────────────────────────┘
```

---

## 快速命令參考

```bash
# 查看狀態
git status                    # 查看當前變更
git status --ignored          # 查看被忽略的文件

# Commit 操作
git add <file>               # 添加文件
git commit -m "message"      # 創建 commit
git commit --amend           # 修改最後一個 commit

# 查看歷史
git log --oneline            # 簡潔歷史
git log --graph --all        # 圖形化歷史
git log --stat               # 帶統計的歷史

# 測試驗證
⌘B                          # Xcode: Build
⌘U                          # Xcode: Run Tests
⌘K                          # Xcode: Clean Build

# 運行自動化腳本
chmod +x git-commit-sequence.sh
./git-commit-sequence.sh
```

---

## 總結

此工作流程圖提供:
- 📊 視覺化的 commit 流程
- 🔄 兩種執行選項的對比
- ⚠️  清晰的測試失敗標註
- ✅ 完整的驗證 checklist
- 🚀 快速命令參考

**建議**: 根據你的需求選擇合適的流程,優先修復 Critical 問題!

---

**版本**: 1.0  
**更新日期**: 2025-12-05  
**狀態**: ✅ 完成
